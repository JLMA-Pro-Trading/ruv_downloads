/**
 * Iris Patterns CLI
 *
 * Command to discover and list cross-project patterns.
 * Usage: iris patterns --source <project> --target <project>
 *
 * @module scripts/iris/iris-patterns
 */
export default function patterns(options: {
    source?: string;
    target?: string;
}): Promise<void>;
//# sourceMappingURL=iris-patterns.d.ts.map