# Iris Platform - Python Dependencies
# Install with: pip install -r requirements.txt

# Bayesian Optimization (Ax Platform)
# Enables 352x faster parameter optimization vs Grid Search
ax-platform>=0.3.0

# DSPy Prompt Optimization
# Enables automatic prompt tuning with MIPROv2
dspy-ai>=2.4.0

# Web framework for Python optimizer service
fastapi>=0.104.0
  uvicorn>=0.24.0
  
  # Optional: Additional optimization backends
  # openai>=1.0.0  # If using OpenAI models# anthropic>=0.7.0  # If using Claude models directly
